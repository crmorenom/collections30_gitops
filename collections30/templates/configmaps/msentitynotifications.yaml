apiVersion: v1
kind: ConfigMap
metadata:
  name: cm-{{ .Values.microservices.msentitynotifications.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/instance: {{ .Values.microservices.msentitynotifications.name }}
    project: {{ .Values.project }}
data:
{{- range $key, $value := .Values.microservices.msentitynotifications.configmap.env }}
  {{ $key }}: {{ $value | quote }}
{{- end }}

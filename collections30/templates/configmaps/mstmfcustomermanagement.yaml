apiVersion: v1
kind: ConfigMap
metadata:
  name: cm-{{ .Values.microservices.mstmfcustomermanagement.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/instance: {{ .Values.microservices.mstmfcustomermanagement.name }}
    project: {{ .Values.project }}
data:
{{- range $key, $value := .Values.microservices.mstmfcustomermanagement.configmap.env }}
  {{ $key }}: {{ $value | quote }}
{{- end }}

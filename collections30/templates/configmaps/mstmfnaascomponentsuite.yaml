apiVersion: v1
kind: ConfigMap
metadata:
  name: cm-{{ .Values.microservices.mstmfnaascomponentsuite.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/instance: {{ .Values.microservices.mstmfnaascomponentsuite.name }}
    project: {{ .Values.project }}
data:
{{- range $key, $value := .Values.microservices.mstmfnaascomponentsuite.configmap.env }}
  {{ $key }}: {{ $value | quote }}
{{- end }}

apiVersion: v1
kind: ConfigMap
metadata:
  name: cm-{{ .Values.microservices.mstmfaccountmanagement.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/instance: {{ .Values.microservices.mstmfaccountmanagement.name }}
    project: {{ .Values.project }}
data:
{{- range $key, $value := .Values.microservices.mstmfaccountmanagement.configmap.env }}
  {{ $key }}: {{ $value | quote }}
{{- end }}

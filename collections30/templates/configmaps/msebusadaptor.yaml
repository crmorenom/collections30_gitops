apiVersion: v1
kind: ConfigMap
metadata:
  name: cm-{{ .Values.microservices.msebusadaptor.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/instance: {{ .Values.microservices.msebusadaptor.name }}
    project: {{ .Values.project }}
data:
{{- range $key, $value := .Values.microservices.msebusadaptor.configmap.env }}
  {{ $key }}: {{ $value | quote }}
{{- end }}


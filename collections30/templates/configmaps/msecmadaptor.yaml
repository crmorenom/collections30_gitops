apiVersion: v1
kind: ConfigMap
metadata:
  name: cm-{{ .Values.microservices.msecmadaptor.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/instance: {{ .Values.microservices.msecmadaptor.name }}
    project: {{ .Values.project }}
data:
{{- range $key, $value := .Values.microservices.msecmadaptor.configmap.env }}
  {{ $key }}: {{ $value | quote }}
{{- end }}


apiVersion: v1
kind: ConfigMap
metadata:
  name: cm-{{ .Values.microservices.schedulermanager.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/instance: {{ .Values.microservices.schedulermanager.name }}
    project: {{ .Values.project }}
data:
{{- range $key, $value := .Values.microservices.schedulermanager.configmap.env }}
  {{ $key }}: {{ $value | quote }}
{{- end }}

apiVersion: v1
kind: ConfigMap
metadata:
  name: cm-{{ .Values.microservices.mserpadaptor.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/instance: {{ .Values.microservices.mserpadaptor.name }}
    project: {{ .Values.project }}
data:
{{- range $key, $value := .Values.microservices.mserpadaptor.configmap.env }}
  {{ $key }}: {{ $value | quote }}
{{- end }}

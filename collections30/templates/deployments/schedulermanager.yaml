apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.microservices.schedulermanager.name }}
  namespace: {{ .Values.namespace }}
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  selector:
    matchLabels:
      run: {{ .Values.microservices.schedulermanager.name }}
  replicas: {{ .Values.microservices.schedulermanager.replicaCount }}
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: {{ .Values.microservices.schedulermanager.name }}
        project: {{ .Values.project }}
        run: {{ .Values.microservices.schedulermanager.name }}
    spec:
      containers:
        - name: {{ .Values.microservices.schedulermanager.name }}
          image: {{ .Values.microservices.schedulermanager.image }}:{{ .Values.microservices.schedulermanager.version }}
          imagePullPolicy: {{ .Values.microservices.schedulermanager.pullPolicy }}
          env:
            - name: JHIPSTER_SECURITY_AUTHENTICATION_JWT_BASE64_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwt-secret-{{ .Values.microservices.schedulermanager.name }}
                  key: secret
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: jwt-secret-{{ .Values.microservices.schedulermanager.name }}
                  key: databasePassword
          envFrom:
            - configMapRef:
                name: cm-{{ .Values.microservices.schedulermanager.name }}
          ports:
            - containerPort: {{ .Values.microservices.schedulermanager.containerPort }}
              protocol: TCP
            - containerPort: {{ .Values.microservices.schedulermanager.configmap.env.APP_HAZELCAST_CONFIG_PORT }}
              protocol: TCP
          volumeMounts:
            - name: tz-config
              mountPath: /etc/localtime
          readinessProbe:
            httpGet:
              path: /services/{{ .Values.microservices.schedulermanager.name }}/management/health
              port: {{ .Values.microservices.schedulermanager.containerPort }}
            initialDelaySeconds: {{ .Values.microservices.schedulermanager.probe.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.microservices.schedulermanager.probe.readiness.periodSeconds }}
            failureThreshold: {{ .Values.microservices.schedulermanager.probe.readiness.failureThreshold }}
          livenessProbe:
            httpGet:
              path: /services/{{ .Values.microservices.schedulermanager.name }}/management/health
              port: {{ .Values.microservices.schedulermanager.containerPort }}
            initialDelaySeconds: {{ .Values.microservices.schedulermanager.probe.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.microservices.schedulermanager.probe.liveness.periodSeconds }}
            failureThreshold: {{ .Values.microservices.schedulermanager.probe.liveness.failureThreshold }}
      volumes:
        - name: tz-config
          hostPath:
            path: /usr/share/zoneinfo/America/Santiago
      imagePullSecrets:
        - name: {{ .Values.ociSecret.name }}

apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: metrics-{{ .Values.microservices.mstmfaccountmanagement.name }}
  namespace: cattle-prometheus
  labels:
    app: {{ .Values.microservices.mstmfaccountmanagement.name }}
    heritage: Tiller
    io.cattle.field/appId: cluster-monitoring
    release: cluster-monitoring
    source: {{ .Values.microservices.mstmfaccountmanagement.name }}
spec:
  endpoints:
  - interval: 30s
    port: http
    path: /services/mstmfaccountmanagement/management/prometheus
  namespaceSelector:
    matchNames:
    - {{ .Values.namespace }}
  selector:
    matchLabels:
       app.kubernetes.io/name: {{ .Values.microservices.mstmfaccountmanagement.name }}

apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: metrics-{{ .Values.microservices.msecmadaptor.name }}
  namespace: cattle-prometheus
  labels:
    app: {{ .Values.microservices.msecmadaptor.name }}
    heritage: Tiller
    io.cattle.field/appId: cluster-monitoring
    release: cluster-monitoring
    source: {{ .Values.microservices.msecmadaptor.name }}
spec:
  endpoints:
  - interval: 30s
    port: http
    path: /services/msecmadaptor/management/prometheus
  namespaceSelector:
    matchNames:
    - {{ .Values.namespace }}
  selector:
    matchLabels:
       app.kubernetes.io/name: {{ .Values.microservices.msecmadaptor.name }}
       
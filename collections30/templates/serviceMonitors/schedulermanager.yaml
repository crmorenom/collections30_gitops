apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: metrics-{{ .Values.microservices.schedulermanager.name }}
  namespace: cattle-prometheus
  labels:
    app: {{ .Values.microservices.schedulermanager.name }}
    heritage: Tiller
    io.cattle.field/appId: cluster-monitoring
    release: cluster-monitoring
    source: {{ .Values.microservices.schedulermanager.name }}
spec:
  endpoints:
  - interval: 30s
    port: http
    path: /services/schedulermanager/management/prometheus
  namespaceSelector:
    matchNames:
    - {{ .Values.namespace }}
  selector:
    matchLabels:
       app.kubernetes.io/name: {{ .Values.microservices.schedulermanager.name }}
       

apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: metrics-{{ .Values.microservices.mschannelsadaptorws.name }}
  namespace: cattle-prometheus
  labels:
    app: {{ .Values.microservices.mschannelsadaptorws.name }}
    heritage: Tiller
    io.cattle.field/appId: cluster-monitoring
    release: cluster-monitoring
    source: {{ .Values.microservices.mschannelsadaptorws.name }}
spec:
  endpoints:
  - interval: 30s
    port: http
    path: /services/mschannelsadaptorws/management/prometheus
  namespaceSelector:
    matchNames:
    - {{ .Values.namespace }}
  selector:
    matchLabels:
       app.kubernetes.io/name: {{ .Values.microservices.mschannelsadaptorws.name }}

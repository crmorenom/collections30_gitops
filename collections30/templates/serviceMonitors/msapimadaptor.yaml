apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: metrics-{{ .Values.microservices.msapimadaptor.name }}
  namespace: cattle-prometheus
  labels:
    app: {{ .Values.microservices.msapimadaptor.name }}
    heritage: Tiller
    io.cattle.field/appId: cluster-monitoring
    release: cluster-monitoring
    source: {{ .Values.microservices.msapimadaptor.name }}
spec:
  endpoints:
  - interval: 30s
    port: http
    path: /services/msapimadaptor/management/prometheus
  namespaceSelector:
    matchNames:
    - {{ .Values.namespace }}
  selector:
    matchLabels:
       app.kubernetes.io/name: {{ .Values.microservices.msapimadaptor.name }}

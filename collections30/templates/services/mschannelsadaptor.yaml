apiVersion: v1
kind: Service
metadata:
  name: svc-{{ .Values.microservices.mschannelsadaptor.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/instance: {{ .Values.microservices.mschannelsadaptor.name }}
    project: {{ .Values.project }}
    app.kubernetes.io/name: {{ .Values.microservices.mschannelsadaptor.name }}
spec:
  type: {{ .Values.microservices.mschannelsadaptor.service.type }}
  ports:
    - port: 80
      name: http
      targetPort: {{ .Values.microservices.mschannelsadaptor.service.port }}
    - port: {{ .Values.microservices.mschannelsadaptor.configmap.env.APP_HAZELCAST_CONFIG_PORT }}
      name: hazelcast       
  selector:
    app.kubernetes.io/instance: {{ .Values.microservices.mschannelsadaptor.name }}

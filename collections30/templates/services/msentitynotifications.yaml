apiVersion: v1
kind: Service
metadata:
  name: svc-{{ .Values.microservices.msentitynotifications.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/instance: {{ .Values.microservices.msentitynotifications.name }}
    project: {{ .Values.project }}
    app.kubernetes.io/name: {{ .Values.microservices.msentitynotifications.name }}
spec:
  type: {{ .Values.microservices.msentitynotifications.service.type }}
  ports:
    - port: 80
      name: http
      targetPort: {{ .Values.microservices.msentitynotifications.service.port }}
    - port: {{ .Values.microservices.msentitynotifications.configmap.env.APP_HAZELCAST_CONFIG_PORT }}
      name: hazelcast 
  selector:
    app.kubernetes.io/instance: {{ .Values.microservices.msentitynotifications.name }}

apiVersion: v1
kind: Service
metadata:
  name: svc-{{ .Values.microservices.gateway.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/instance: {{ .Values.microservices.gateway.name }}
    project: {{ .Values.project }}
    app.kubernetes.io/name: {{ .Values.microservices.gateway.name }}
spec:
  type: {{ .Values.microservices.gateway.service.type }}
  ports:
    - port: 80
      name: http
      targetPort: {{ .Values.microservices.gateway.service.port }}
    - port: {{ .Values.microservices.gateway.configmap.env.APP_HAZELCAST_CONFIG_PORT }}
      name: hazelcast 
  selector:
    app.kubernetes.io/instance: {{ .Values.microservices.gateway.name }}

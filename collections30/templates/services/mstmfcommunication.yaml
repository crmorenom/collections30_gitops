apiVersion: v1
kind: Service
metadata:
  name: svc-{{ .Values.microservices.mstmfcommunication.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/instance: {{ .Values.microservices.mstmfcommunication.name }}
    project: {{ .Values.project }}
    app.kubernetes.io/name: {{ .Values.microservices.mstmfcommunication.name }}
spec:
  type: {{ .Values.microservices.mstmfcommunication.service.type }}
  ports:
    - port: 80
      name: http
      targetPort: {{ .Values.microservices.mstmfcommunication.service.port }}
  selector:
    app.kubernetes.io/instance: {{ .Values.microservices.mstmfcommunication.name }}

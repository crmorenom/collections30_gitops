apiVersion: v1
kind: Service
metadata:
  name: svc-{{ .Values.microservices.mstmfaccountmanagement.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/instance: {{ .Values.microservices.mstmfaccountmanagement.name }}
    project: {{ .Values.project }}
    app.kubernetes.io/name: {{ .Values.microservices.mstmfaccountmanagement.name }}
spec:
  type: {{ .Values.microservices.mstmfaccountmanagement.service.type }}
  ports:
    - port: 80
      name: http
      targetPort: {{ .Values.microservices.mstmfaccountmanagement.service.port }}
  selector:
    app.kubernetes.io/instance: {{ .Values.microservices.mstmfaccountmanagement.name }}

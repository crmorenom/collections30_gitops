apiVersion: v1
kind: Service
metadata:
  name: svc-{{ .Values.microservices.msebusadaptor.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/instance: {{ .Values.microservices.msebusadaptor.name }}
    project: {{ .Values.project }}
    app.kubernetes.io/name: {{ .Values.microservices.msebusadaptor.name }}
spec:
  type: {{ .Values.microservices.msebusadaptor.service.type }}
  ports:
    - port: 80
      name: http
      targetPort: {{ .Values.microservices.msebusadaptor.service.port }}
  selector:
    app.kubernetes.io/instance: {{ .Values.microservices.msebusadaptor.name }}

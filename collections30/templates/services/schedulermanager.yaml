apiVersion: v1
kind: Service
metadata:
  name: svc-{{ .Values.microservices.schedulermanager.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/instance: {{ .Values.microservices.schedulermanager.name }}
    project: {{ .Values.project }}
    app.kubernetes.io/name: {{ .Values.microservices.schedulermanager.name }}
spec:
  type: {{ .Values.microservices.schedulermanager.service.type }}
  ports:
    - port: 80
      name: http
      targetPort: {{ .Values.microservices.schedulermanager.service.port }}
    - port: {{ .Values.microservices.schedulermanager.configmap.env.APP_HAZELCAST_CONFIG_PORT }}
      name: hazelcast 
  selector:
    app.kubernetes.io/instance: {{ .Values.microservices.schedulermanager.name }}
